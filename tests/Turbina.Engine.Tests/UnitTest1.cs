using System;
using System.Threading;
using System.Threading.Tasks;
using Xunit;

namespace Turbina.Engine.Tests
{
    public class UnitTest1
    {
//        [Fact]
//        public async void Test1()
//        {
//            using (var cts = new CancellationTokenSource())
//            {
//                var buffer = new InletBuffer(CancellationToken.None, 200, "tmp");
//                Task.Run(() =>
//                {
//                    int i = 0;
//                    while (true)
//                    {
//                        buffer.Post(++i);
//                    }
//                });
//                await buffer.OutputAvailableAsync(cts.Token, false);
//                await buffer.OutputAvailableAsync(cts.Token, false);
//                await buffer.OutputAvailableAsync(cts.Token, false);
//                await buffer.OutputAvailableAsync(cts.Token, false);
//                await buffer.OutputAvailableAsync(cts.Token, false);
//                var i6 = await buffer.TakeAsync(cts.Token, false);
//                var i7 = await buffer.TakeAsync(cts.Token, false);
//            }
//        }
    }
}
